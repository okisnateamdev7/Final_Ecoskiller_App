spring:
  application:
    name: mcp-01-platform

  ai:
    mcp:
      server:
        name: mcp-01-platform
        version: 1.0.0
        type: ASYNC           # SYNC for stdio, ASYNC for HTTP/SSE
        sse-message-endpoint: /mcp/message
        resource-change-notification: true
        tool-change-notification: true

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.ecoskiller: DEBUG
    org.springframework.ai.mcp: INFO
