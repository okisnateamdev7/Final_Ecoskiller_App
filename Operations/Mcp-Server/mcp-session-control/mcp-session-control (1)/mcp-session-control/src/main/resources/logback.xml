<configuration>
    <!-- All output → STDERR. stdout is reserved for JSON-RPC MCP protocol. -->
    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Dedicated AUDIT appender — structured JSON for Wazuh SIEM -->
    <appender name="AUDIT_STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder><pattern>[AUDIT] %msg%n</pattern></encoder>
    </appender>

    <logger name="AUDIT"                     level="INFO" additivity="false">
        <appender-ref ref="AUDIT_STDERR"/>
    </logger>
    <logger name="com.ecoskiller.mcp"        level="${LOG_LEVEL:-INFO}"/>

    <root level="WARN">
        <appender-ref ref="STDERR"/>
    </root>
</configuration>
